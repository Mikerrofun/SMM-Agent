-- Добавляет статус отменённого прогона пайплайна.
-- ВАЖНО: ALTER TYPE ... ADD VALUE нельзя выполнять в той же транзакции,
-- где значение употребляется. Файл содержит единственное выражение —
-- не доклеивать другие выражения. Применяется вручную владельцем проекта.
ALTER TYPE "RunStatus" ADD VALUE 'CANCELLED';
